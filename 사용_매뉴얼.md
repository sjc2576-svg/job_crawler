# 채용 공고 수집 시스템 사용 매뉴얼

사람인에서 원하는 조건(직무/지역/경력/학력/근무형태)의 채용공고를 자동으로 수집해서
DB에 저장하고, 웹 화면에서 필터링하며 확인할 수 있는 시스템입니다.

---

## 1. 폴더 구조

```
job_crawler/
├── config.py           # DB 설정, 조건 코드표, 스케줄 설정
├── crawler.py          # 크롤러 (실행 파일)
├── scheduler.py         # 자동 스케줄링 (실행 파일)
├── database.py         # DB 연결/저장/조회
├── app.py               # Flask 웹 서버 (실행 파일)
├── requirements.txt      # 필요한 패키지 목록
└── templates/
    └── index.html        # 웹 화면
```

---

## 2. 처음 설치하기

### 2-1. MySQL 준비

1. MySQL이 설치되어 있어야 합니다.
2. `jobs`라는 이름의 데이터베이스를 만들어주세요.

```sql
CREATE DATABASE jobs CHARACTER SET utf8mb4;
```

3. `config.py`의 `MYSQL_CONFIG`에 본인 환경에 맞는 접속 정보를 입력하세요.

```python
MYSQL_CONFIG = {
    "host": "localhost",
    "user": "root",
    "password": "본인 비밀번호",
    "database": "jobs"
}
```

> 테이블(`job_posting`)은 `crawler.py`나 `app.py`를 처음 실행할 때 자동으로 생성됩니다.
> 따로 테이블을 만들어둘 필요는 없습니다.

### 2-2. 파이썬 패키지 설치

프로젝트 폴더에서 아래 명령을 실행하세요.

```bash
pip install -r requirements.txt
```

### 2-3. 크롬 브라우저

Selenium이 크롬을 직접 제어하므로, 컴퓨터에 크롬 브라우저가 설치되어 있어야 합니다.
드라이버는 `webdriver-manager`가 실행 시 자동으로 받아오므로 따로 설치할 필요는 없습니다.

---

## 3. 크롤러 실행하기 (`crawler.py`)

```bash
python crawler.py
```

실행하면 터미널에서 조건을 순서대로 물어봅니다.

```
검색 조건을 직접 입력해주세요.

[직무 선택]
  1. 생산
  2. 기획전략
  3. 총무법무사무
  4. 구매자재물류
직무 번호 입력 (엔터=1번 기본값):
```

- 숫자를 입력하고 엔터를 누르면 해당 항목이 선택됩니다.
- 그냥 엔터만 누르면 **1번(보통 "전체")** 이 기본값으로 선택됩니다.
- 직무 → 지역 → 경력 → 학력 → 근무형태 순서로 5번 물어봅니다.

조건 하나를 다 입력하면 이렇게 나옵니다.

```
>> 설정된 조건: 울산 생산 (전체/전체/정규직)

조건을 더 추가하시겠습니까? (y/N):
```

- `y`를 입력하면 또 다른 조건을 이어서 입력할 수 있습니다. (예: 울산 생산직 + 경남 생산직을 한 번에 수집)
- 그냥 엔터를 누르면 입력을 마치고 크롤링을 시작합니다.

조건 입력이 끝나면, 기존에 저장된 공고가 있을 경우 삭제 여부를 한 번 더 물어봅니다.

```
기존에 저장된 채용공고가 66건 있습니다. 전부 삭제하고 새로 시작할까요? (상태/메모도 함께 삭제됩니다) (y/N):
```

- `y`를 입력하면 기존 데이터(지원 상태/메모 포함)를 **전부 삭제**하고 새로 수집합니다. 한 번 삭제하면 복구할 수 없습니다.
- `y` 이외의 값(그냥 엔터 포함)을 입력하면 기존 데이터는 그대로 두고, 새로 찾은 공고만 추가로 저장합니다.
- 그냥 테스트 삼아 조건 하나만 돌려볼 때는 반드시 `N`(또는 그냥 엔터)을 눌러서 기존 데이터를 보존하세요.

### 3-1. 크롤링 진행 화면

조건마다 아래와 같은 로그가 출력됩니다.

```
=== [울산 생산 (전체/전체/정규직)] 크롤링 시작 ===
URL: https://www.saramin.co.kr/zf_user/jobs/list/job-category?...
[울산 생산 (전체/전체/정규직)] 1페이지: 발견 20건 / 신규 저장 18건
[울산 생산 (전체/전체/정규직)] 2페이지: 발견 20건 / 신규 저장 20건
...
[울산 생산 (전체/전체/정규직)] 마지막 페이지로 판단, 다음 조건으로 이동
[울산 생산 (전체/전체/정규직)] 조건 합계: 신규 저장 87건 / 발견 90건
```

- **최대 5페이지**까지 자동으로 "다음" 버튼을 눌러가며 수집합니다 (`crawler.py` 상단 `MAX_PAGES` 값으로 조절 가능).
- 이미 저장된 공고(같은 링크)는 자동으로 건너뛰므로, **여러 번 실행해도 중복 저장되지 않습니다.**
- 타임아웃 등으로 페이지 로딩에 실패하면 최대 2번까지 재시도하고, 그래도 안 되면 해당 조건만 건너뛰고 다음 조건으로 넘어갑니다.

### 3-2. 조건 커스터마이징

`config.py`의 `JOB_CATEGORY`, `LOCATION`, `EXPERIENCE`, `EDUCATION`, `JOB_TYPE` 딕셔너리에
항목을 추가하면 실행 시 메뉴에도 그대로 반영됩니다.

```python
JOB_CATEGORY = {
    "생산": 11,
    "기획전략": 1,
    "IT개발데이터": 6,   # <- 이렇게 추가하면 메뉴에 자동으로 나타남
}
```

---

## 4. 웹 화면으로 확인하기 (`app.py`)

크롤링이 끝난 뒤, 아래 명령으로 웹 서버를 실행합니다.

```bash
python app.py
```

브라우저에서 아래 주소로 접속하세요.

```
http://127.0.0.1:5000
```

### 4-1. 화면 구성

- **지역** 입력창: "울산"처럼 입력하면 지역명에 해당 문자열이 포함된 공고만 표시
- **회사명/제목 검색** 입력창: 회사명이나 공고 제목에 포함된 단어로 검색
- **수집 조건** 드롭다운: `crawler.py` 실행 시 입력했던 조건(예: "울산 생산 (전체/전체/정규직)")별로 필터링
- **상태** 드롭다운: 아래 4-2의 지원 현황(미확인/관심있음/지원함/제외)으로 필터링
- **필터 적용** 버튼을 누르면 조건에 맞는 공고만 표로 보여줍니다.
- **초기화** 링크를 누르면 전체 목록으로 돌아갑니다.

### 4-2. 지원 현황 관리 (상태 / 메모)

각 공고 행에 있는 **상태** 드롭다운에서 아래 4가지 중 하나를 선택하면 즉시 저장됩니다.

- 미확인 (기본값)
- 관심있음
- 지원함 — 이 상태로 처음 바꾸는 시점의 날짜가 **지원일** 칸에 자동으로 기록됩니다.
- 제외

**메모** 칸에 원하는 내용(면접 일정, 연봉 협의 내용 등)을 입력하고 "저장" 버튼을 누르면 해당 공고에 메모가 남습니다.

### 4-3. 회사별로 묶어보기 (유사/중복 공고 정리)

같은 회사가 비슷한 공고를 여러 건 올리는 경우가 많아서, 필터 폼 아래의 **회사별 보기** 탭을 누르면
회사명별로 공고를 묶어서 "회사명 (N건)" 형태로 보여줍니다. 다시 **목록 보기** 탭을 누르면 원래의
공고 하나씩 나열하는 화면으로 돌아갑니다. 현재 적용된 지역/키워드/조건/상태 필터는 두 화면 모두에 그대로 유지됩니다.

---

## 5. 자동 스케줄링 (`scheduler.py`)

매일 손으로 `python crawler.py`를 실행하지 않아도, `scheduler.py`를 켜두면
정해진 시각에 자동으로 새 공고만 수집합니다.

### 5-1. 준비

1. `config.py`의 `SEARCH_CONDITIONS`에 자동으로 수집할 조건을 미리 등록해두세요.
   자동 실행은 터미널로 물어보지 않고 이 목록을 그대로 사용합니다.

```python
SEARCH_CONDITIONS = [
    {
        "name": "울산 생산직 (정규직)",
        "cat_mcls": JOB_CATEGORY["생산"],
        "loc_mcd": LOCATION["울산"],
        "exp_cd": None,
        "edu_lv": None,
        "job_type": JOB_TYPE["정규직"],
    },
    # 필요한 만큼 조건을 추가하세요.
]
```

2. `config.py`의 `SCHEDULE_TIME`으로 매일 실행할 시각을 정합니다 (24시간제, 예: `"09:00"`).
   **선택 사항입니다.** 기본값은 `None`이며, `None`으로 두면 `scheduler.py`를 실행해도
   아무 것도 하지 않고 바로 종료됩니다. 자동 스케줄링을 쓰고 싶을 때만 시각을 지정하세요.

```python
SCHEDULE_TIME = "09:00"
```

### 5-2. 실행

```bash
python scheduler.py
```

- `SCHEDULE_TIME`을 설정하지 않았다면(기본값 `None`) 이 명령을 실행해도 아무 일도 일어나지 않습니다.
- `SCHEDULE_TIME`을 설정했다면, 실행 시 매일 그 시각에 자동으로 크롤링이 돌아갑니다.
- **대화형 `crawler.py`와 달리 기존 데이터를 지우지 않고, 새로 발견된 공고만 추가로 저장**합니다.
  (그래서 지원 현황 화면에서 남겨둔 상태/메모가 자동 실행 때문에 사라지지 않습니다.)
- 이 프로세스가 떠 있는 동안만 동작하므로, **터미널 창을 계속 열어둬야 합니다.** 창을 닫으면 스케줄이 멈춥니다.
- 종료하려면 해당 터미널에서 `Ctrl+C`를 누르세요.

### 5-3. 실행 기록 확인

크롤링 진행 상황(신규 저장 건수, 오류 등)은 콘솔뿐 아니라 프로젝트 폴더의 **`crawler.log`** 파일에도 남습니다.
스케줄러를 백그라운드로 켜두고 나중에 결과만 확인하고 싶을 때 이 파일을 열어보면 됩니다.
(파일이 너무 커지지 않도록 일정 크기가 넘으면 자동으로 백업/교체됩니다.)

> 현재는 알림 기능(이메일/디스코드 등) 없이 로그 파일로만 기록합니다. 나중에 필요해지면
> `crawler.py`의 `run_scheduled()` 안에서 신규 저장 건수가 있을 때 원하는 채널로 알림을 보내도록 추가할 수 있습니다.

---

## 6. 자주 발생할 수 있는 문제

| 증상 | 원인 / 해결 방법 |
|---|---|
| `DB ERROR` 문구가 뜸 | `config.py`의 MySQL 접속 정보 확인, MySQL 서버가 켜져 있는지 확인 |
| 크롤링 중 계속 타임아웃 | 사람인 페이지 구조가 바뀌었을 가능성. `debug_timeout_*.png` 스크린샷 확인 후 알려주면 selector 수정 가능 |
| 페이지가 다음으로 안 넘어감 | "다음" 버튼 텍스트/구조가 바뀌었을 수 있음. `crawler.py`의 `go_to_next_page()` 함수 selector 재확인 필요 |
| 경력/학력/근무형태 조건이 실제로 안 먹히는 것 같음 | 사람인 웹페이지의 실제 파라미터명이 `config.py`와 다를 수 있음. 사이트에서 직접 필터를 걸어보고 바뀌는 URL을 확인해서 알려주면 반영 가능 |
| 같은 공고가 계속 다시 저장됨 | DB에 `link` 컬럼 UNIQUE 인덱스가 정상 생성됐는지 확인 (기존에 중복 데이터가 있으면 인덱스 생성이 실패할 수 있음 → 기존 데이터 정리 필요) |

---

## 7. 주의사항

- 너무 자주, 너무 많은 페이지를 짧은 시간에 반복 요청하면 IP가 일시적으로 차단될 수 있습니다. `MAX_PAGES`를 무리하게 늘리지 마세요.
- 개인 학습/포트폴리오 목적의 크롤링이며, 수집한 데이터를 상업적으로 재배포하지 않는 것을 권장합니다.

